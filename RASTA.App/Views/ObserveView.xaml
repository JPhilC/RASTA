<UserControl x:Class="RASTA.App.Views.ObserveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            <StackPanel>

            <TextBlock Text="Observe (Capture)"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>

            <Button Content="Capture Single"
                    Width="200"
                    Height="40"
                    Command="{Binding CaptureSingleCommand}"/>

            <Button Content="Capture Sweep"
                    Width="200"
                    Height="40"
                    Margin="0 10 0 0"
                    Command="{Binding CaptureSweepCommand}"/>

            <TextBlock Text="Last Observation:"
                       FontWeight="Bold"
                       Margin="0 20 0 5"/>

            <TextBlock Text="{Binding LastObservation.Metadata.Pointing.AzimuthDeg,
                              StringFormat=Az: {0}}"/>

            <TextBlock Text="{Binding LastObservation.Metadata.Pointing.ElevationDeg,
                              StringFormat=El: {0}}"/>

        </StackPanel>
        <StackPanel Grid.Row="1">

            <TextBlock Text="Test" 
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>

            <Button Content="Test RAW Capture"
                    Width="180"
                    Height="40"
                    Command="{Binding TestRawCaptureCommand}" />

            <TextBlock Text="Last Capture:"
                       Margin="0 20 0 5"
                       FontWeight="SemiBold"/>

            <TextBlock Text="{Binding LastCapturePath}"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</UserControl>
