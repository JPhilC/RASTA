<UserControl x:Class="RASTA.App.Views.MosaicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:RASTA.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="9"
                   Text="Mosaic (multi-capture)"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Folder / baseline row -->
        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"
                   Text="Session Folder" FontWeight="Bold"/>
        <TextBox Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="5" MinWidth="300"
                 VerticalAlignment="Center"
                 Text="{Binding CaptureFolder, Mode=OneWay}" IsReadOnly="True"
                 HorizontalAlignment="Left" Margin="0,0,5,0"/>
        <Button Grid.Row="2" Grid.Column="8"
                Content="Select Folder…" Command="{Binding SelectFolderCommand}"
                Padding="10,4" Width="160"/>

        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"
                   Text="Baseline File" FontWeight="Bold"/>
        <TextBox Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="5" MinWidth="300"
                 VerticalAlignment="Center"
                 Text="{Binding BaselineFile, Mode=OneWay}" IsReadOnly="True"
                 HorizontalAlignment="Left" Margin="0,0,5,0"/>
        <Button Grid.Row="4" Grid.Column="8"
                Content="Select Baseline FIT" Command="{Binding SelectBaselineFileCommand}"
                Visibility="{Binding BaselineAvailable, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                Padding="10,4" Width="160"/>
        <Button Grid.Row="4" Grid.Column="8"
                Content="Clear Baseline FIT" Command="{Binding ClearBaselineFileCommand}"
                Visibility="{Binding BaselineAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"
                Padding="10,4" Width="160"/>

        <!-- Parameters row -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="9" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,10">
            <TextBlock Text="Target FFT Size" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="60" Text="{Binding TargetFftSize}" VerticalAlignment="Center"/>

            <TextBlock Text="Integrated Window (km/s)" FontWeight="Bold" VerticalAlignment="Center" Margin="15,0,5,0"/>
            <TextBox Width="60" Text="{Binding IntegratedWindowKmPerSec}" VerticalAlignment="Center"/>

            <TextBlock Text="Sky Cell Size (°)" FontWeight="Bold" VerticalAlignment="Center" Margin="15,0,5,0"
                       ToolTip="Should match the sweep's own step size, in degrees, so each pixel is one real sky cell"/>
            <TextBox Width="60" Text="{Binding SkyCellSizeDeg}" VerticalAlignment="Center"/>

            <Button Content="Generate Mosaic" Command="{Binding GenerateMosaicCommand}"
                    Padding="10,4" Width="160" Margin="20,0,0,0"/>

            <TextBlock Text="{Binding StatusSummary}" VerticalAlignment="Center" Margin="15,0,0,0" FontStyle="Italic"/>
        </StackPanel>

        <!-- Results -->
        <Grid Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="9">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TabControl Grid.Row="0" Grid.Column="0">
                <TabItem Header="Sky Mosaic">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                BorderBrush="Gray" BorderThickness="1" HorizontalAlignment="Center">
                                <Image Source="{Binding SkyHeatmap.Image}" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"
                                   MaxWidth="960" MaxHeight="520"/>
                        </Border>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                            <TextBlock Text="{Binding SkyHeatmap.YAxisLabel}" Margin="0,0,15,0"/>
                            <TextBlock Text="{Binding SkyHeatmap.XAxisLabel}"/>
                            <CheckBox Content="Smooth blend" IsChecked="{Binding UseSmoothBlend}"
                                      VerticalAlignment="Center" Margin="25,0,0,0"
                                      ToolTip="Bilinear-interpolate between neighbouring cell centers instead of one flat colour per measured cell"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0">
                            <TextBlock Text="{Binding SkyHeatmap.LegendMinText}" VerticalAlignment="Center"/>
                            <Image Source="{Binding SkyHeatmap.LegendImage}" Width="200" Height="14" Margin="6,0,6,0"
                                   RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                            <TextBlock Text="{Binding SkyHeatmap.LegendMaxText}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </TabItem>
                <TabItem Header="3D Surface">
                    <views:MosaicSurfaceView IntensityGrid="{Binding SurfaceIntensityGrid}"
                                              XValues="{Binding SurfaceXValues}"
                                              YValues="{Binding SurfaceYValues}"/>
                </TabItem>
            </TabControl>

            <DataGrid Grid.Row="0" Grid.Column="2" ItemsSource="{Binding Positions}" AutoGenerateColumns="False"
                      IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Position" Binding="{Binding Label}" Width="*"/>
                    <DataGridTextColumn Header="Coordinates" Binding="{Binding Coordinates}" Width="2*"/>
                    <DataGridTextColumn Header="Line Strength (dB)" Binding="{Binding LineStrengthDb, StringFormat=F1}" Width="*"/>
                    <DataGridTextColumn Header="Files" Binding="{Binding FileCount}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
