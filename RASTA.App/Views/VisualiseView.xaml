<UserControl x:Class="RASTA.App.Views.VisualiseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Spectrum"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>

            <Image Source="{Binding SpectrumImage}"
                   Stretch="Uniform"
                   Height="400"/>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <TextBlock Text="Heatmap"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>

            <Image Source="{Binding HeatmapImage}"
                   Stretch="Uniform"
                   Height="400"/>

            <CheckBox Content="Use Gridding"
                      Margin="0 20 0 0"
                      IsChecked="{Binding UseGridding}"/>
        </StackPanel>
    </Grid>
</UserControl>
