<Window x:Class="RASTA.App.Views.ColdSkySelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Choose a Cold-Sky Position"
        Width="720" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Pick a cold-sky position for the baseline capture"
                   FontSize="20" FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,5"/>

        <TextBlock Grid.Row="1"
                   Text="These positions are clear of the Galactic plane (low HI emission) and spread across the sky - pick whichever is conveniently unobstructed. If none look workable, use Recalculate for a fresh set of suggestions."
                   Foreground="#BBBBBB"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <ItemsControl Grid.Row="2" x:Name="CandidateList">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2A2A2A"
                            BorderBrush="#444444" BorderThickness="1"
                            CornerRadius="4"
                            Margin="0,0,0,10" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Label}"
                                           FontWeight="Bold" FontSize="15"
                                           Foreground="#4FC3F7"/>
                                <TextBlock Text="{Binding AzAltText}" Foreground="White" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding RaDecText}" Foreground="White"/>
                                <TextBlock Text="{Binding GalacticText}" Foreground="#BBBBBB" Margin="0,4,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="1"
                                    Content="Select"
                                    Width="90" Height="34"
                                    VerticalAlignment="Center"
                                    Click="SelectButton_Click"
                                    Tag="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button Content="Recalculate"
                    Width="110" Height="34"
                    Margin="0,0,10,0"
                    Click="RecalculateButton_Click"/>
            <Button Content="Cancel Calibration"
                    Width="150" Height="34"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
